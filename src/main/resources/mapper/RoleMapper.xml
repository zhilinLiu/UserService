<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kando.mapper.RoleDao">

    <resultMap type="com.kando.entity.Role" id="RoleMap">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="description" property="description"/>
        <result column="status" property="status"/>
        <result column="create_time" javaType="java.sql.Date" property="create_time"/>
        <result column="create_user_id" property="create_user_id"/>
    </resultMap>

    <select id="queryRolesByPhone" resultMap="RoleMap">
        select
            role.id,role.name,role.description,role.status,role.create_time,role.create_user_id
        from
            sys_user user,sys_user_role ur,sys_role role
        where
            user.phone = #{phone}
        and
            user.id = ur.user_id
        and
            ur.role_id = role.id
    </select>
    <select id="queryAllRoles" resultMap="RoleMap">
        select role.id,role.name,role.description,role.status,role.create_time,role.create_user_id from sys_role role ORDER BY create_time desc
    </select>
    <select id="queryAllRolesLike" resultMap="RoleMap">
        select * from sys_role where name like #{field} ORDER BY create_time desc
    </select>
    <select id="queryAll" resultMap="RoleMap">
        select * from sys_role
    </select>
    <select id="selectRole" resultMap="RoleMap">
        select * from sys_role where id=#{id}
    </select>
    <select id="queryUserUnderRole" resultMap="RoleMap">
        select user.user_name  from sys_user_role ur,sys_user user where ur.role_id = #{roleid} and ur.user_id = user.id
    </select>
    <select id="queryUserUnderRoleAll" resultType="java.lang.String">
        select user.user_name  from sys_user_role ur,sys_user user where ur.user_id=user.id
    </select>
    <insert id="addUserRoles" parameterType="java.lang.String">
        delete from sys_user_role where user_id=(select id from sys_user where phone=#{phone}) and role_id=#{num}
    </insert>
    <delete id="deleteUserRoles" >
        delete from sys_user_role where user_id=(select id from sys_user where phone=#{phone}) and role_id=#{num}
    </delete>
    <insert id="inserRole" parameterType="com.kando.entity.Role">
        INSERT INTO sys_role(name,description,create_time,create_user_id,status) values(#{name},#{description},current_timestamp(),#{create_user_id},#{status})
    </insert>
    <delete id="deleteRole" parameterType="java.lang.Integer">
        delete  from sys_role where id=#{id}
    </delete>
    <update id="updateRole" parameterType="com.kando.entity.Role">
        update sys_role set name=#{name},description=#{description},status=#{status} where id=#{id}
    </update>

</mapper>